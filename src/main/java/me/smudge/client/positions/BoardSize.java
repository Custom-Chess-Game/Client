package me.smudge.client.positions;

/**
 * Used to set the size of the board
 */
public class BoardSize extends Position {

    /**
     * Create a new instance of a board size as {@link Position}
     * @param x The x index
     * @param y The y index
     */
    public BoardSize(int x, int y) {
        super(x, y);
    }
}
